{{!< default}}
{{!-- The tag above means - insert everything in this file into the {body} of the default.hbs template --}}

{{!-- The big featured header --}}
<!-- header class="main-header {{#if @blog.cover}}" style="background-image: url({{@blog.cover}}){{else}}no-cover{{/if}}">
    <nav class="main-nav overlay clearfix">
        {{#if @blog.logo}}<a class="blog-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>{{/if}}
        {{#if @blog.navigation}}
            <a class="menu-button icon-menu" href="#"><span class="word">Menu</span></a>
        {{/if}}
    </nav>
    <div class="vertical">
        <div class="main-header-content inner">
            <h1 class="page-title">{{@blog.title}}</h1>
            <h2 class="page-description">{{@blog.description}}</h2>
        </div>
    </div>
    <a class="scroll-down icon-arrow-left" href="#content" data-offset="-45"><span class="hidden">Scroll Down</span></a>
</header -->

<header class="custom-header">
    <div class="container">
        <nav class="navbar">
            <!-- a class="nav-logo" href="{{@blog.url}}">
                <img src="{{@blog.logo}}" alt="{{@blog.title}}" />
            </a -->
            <div class="nav-text-logo">
                [hi@<a href="{{@blog.url}}">longgeek.com</a>]$ sudo su -
            </div>
            {{#if @blog.navigation}}
                {{navigation}}
            {{/if}}
            </ul>
            <a class="nav-collapse icon-menu"></a>
        </nav>
        <div class="navbar-collapse">
            <div class="divider2"></div>
            {{#if @blog.navigation}}
                {{navigation}}
            {{/if}}
            <div class="divider3"></div>
        </div>
    </div>
</header>

{{!-- The main content area on the homepage --}}
<main id="content" class="content" role="main">
    <div class="container">
        <div class="work-section">
            <!-- ul>
                {{#get "tags" limit="all"}}
                  {{#foreach tags}}
                    <li><a href="{{url}}">{{name}}{{total}}</a></li>
                  {{/foreach}}
                {{/get}}
            </ul -->
            <div class="work-logo">
                <img src="/assets/home-smile.svg" alt="">
            </div>
            <div class="work-info">
                <p>
                    Hi, friends! I am Longgeek, <br>
                    is aslo a Developer.
                    I now work in fuvism.com.<br>
                </p>
            </div>
            <div class="work-link"></div>
        </div>
    </div>
    <div class="fuvism-bar">
        <div class="container">
            Know what I'm doing now
            <br/>
            <br/>
            <button><a href="http://www.fuvism.com/" target="_blank">www.fuvism.com</a></button>
        </div>
    </div>
</main>
